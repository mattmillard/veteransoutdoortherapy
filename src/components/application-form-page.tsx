import { IntakeForm } from "./intake-form";
export function ApplicationFormPage({ eyebrow, title, copy, type }: { eyebrow: string; title: string; copy: string; type: string }) { return <><section className="page-hero compact-hero"><div className="container"><p className="eyebrow">{eyebrow}</p><h1 className="display">{title}</h1><p>{copy}</p></div></section><section className="section"><div className="container form-layout"><div><p className="eyebrow">Confidential application</p><h2 className="display section-title">Tell us about you.</h2><p>There is no cost to participate in selected programs. Your information is used only to review and coordinate this request.</p></div><IntakeForm kind={type} includeType={false} /></div></section></>; }