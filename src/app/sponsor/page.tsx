import { Check } from "lucide-react";
import Link from "next/link";
import { getProducts } from "@/lib/db";
import type { Metadata } from "next";
export const metadata: Metadata = { title: "Sponsorships" };
const benefits = ["Recognition on mission channels", "Event and community connection", "Direct impact for Veterans", "Annual appreciation invitation"];
export default async function SponsorPage() { const levels = (await getProducts()).filter((item) => item.category === "Sponsorships" && item.slug !== "custom-sponsor").reverse(); return <><section className="page-hero sponsor-hero"><div className="container"><p className="eyebrow">Empower our heroes through nature</p><h1 className="display">Put your support<br />in the field.</h1><p>Your organization can remove the cost between America&apos;s heroes and an experience that restores confidence, connection, and peace.</p></div></section><section className="section"><div className="container"><p className="eyebrow">Sponsorship tiers</p><h2 className="display section-title">Choose your impact.</h2><div className="sponsor-grid">{levels.map((level) => <article key={level.slug}><p className="eyebrow">{level.shortName}</p><h2 className="display">${level.price.toLocaleString()}+</h2>{benefits.map((benefit) => <span key={benefit}><Check size={17} />{benefit}</span>)}<Link className="button" href={`/product/${level.slug}`}>Choose {level.shortName}</Link></article>)}</div></div></section><section className="sponsor-proof"><div className="container"><div><p className="eyebrow">What sponsorship funds</p><h2 className="display">Travel. Gear. Meals. Time together.</h2></div><p>Every level helps cover the practical details behind fully funded hunts, fishing trips, horseback camps, hikes, and community events.</p><Link className="button orange" href="/contact">Build a custom partnership</Link></div></section></>; }