import { Check } from "lucide-react";
import Link from "next/link";
import { products } from "@/lib/data";
const benefits = ["Recognition on mission channels", "Event and community connection", "Direct impact for Veterans", "Annual appreciation invitation"];
export default function SponsorPage() { const levels = products.filter((item) => item.category === "Sponsorships" && item.slug !== "custom-sponsor").reverse(); return <section className="section"><div className="container"><p className="eyebrow">Back the next adventure</p><h1 className="display section-title">Put your support<br />in the field.</h1><p className="prose">Sponsorships fund travel, gear, meals, and the logistics that make fully supported experiences possible.</p><div className="sponsor-grid">{levels.map((level) => <article key={level.slug}><p className="eyebrow">{level.shortName}</p><h2 className="display">${level.price.toLocaleString()}+</h2>{benefits.map((benefit) => <span key={benefit}><Check size={17} />{benefit}</span>)}<Link className="button" href={`/product/${level.slug}`}>Choose {level.shortName}</Link></article>)}</div></div></section>; }