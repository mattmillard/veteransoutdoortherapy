import { NextResponse } from "next/server";
async function accessToken() { const credentials = Buffer.from(`${process.env.PAYPAL_CLIENT_ID}:${process.env.PAYPAL_CLIENT_SECRET}`).toString("base64"); const response = await fetch(`${process.env.PAYPAL_API_BASE || "https://api-m.sandbox.paypal.com"}/v1/oauth2/token`, { method: "POST", headers: { Authorization: `Basic ${credentials}`, "Content-Type": "application/x-www-form-urlencoded" }, body: "grant_type=client_credentials" }); return (await response.json() as { access_token: string }).access_token; }
export async function POST(request: Request) { try { const { orderID } = await request.json() as { orderID: string }; if (!/^[A-Z0-9]+$/i.test(orderID)) return NextResponse.json({ error: "Invalid order" }, { status: 400 }); const token = await accessToken(); const response = await fetch(`${process.env.PAYPAL_API_BASE || "https://api-m.sandbox.paypal.com"}/v2/checkout/orders/${orderID}/capture`, { method: "POST", headers: { Authorization: `Bearer ${token}`, "Content-Type": "application/json" } }); return NextResponse.json(await response.json(), { status: response.status }); } catch { return NextResponse.json({ error: "Unable to capture PayPal order" }, { status: 500 }); } }