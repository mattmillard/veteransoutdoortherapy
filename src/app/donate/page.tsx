import Link from "next/link";
export default function DonatePage() { return <section className="donate-page"><div className="container"><p className="eyebrow">Every gift moves the mission</p><h1 className="display">Fuel the<br />next mile.</h1><p>From a warm meal on a hunt to fuel for a fishing trip, your contribution gives Veterans space to reconnect and heal outside.</p><div className="donation-options">{[25,50,100,250].map((amount) => <Link key={amount} href={`/product/custom-sponsor?amount=${amount}`}>${amount}</Link>)}</div><Link className="button orange" href="/product/custom-sponsor">Choose a custom gift</Link></div></section>; }